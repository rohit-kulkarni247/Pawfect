<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petlify">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Petlify">
        <activity android:name=".Interaction"></activity>
        <activity android:name=".CampOfPets">

        </activity>
        <activity android:name=".CampDetails"></activity>
        <activity android:name=".AnimalDetails" />
        <activity android:name=".Profile" />
        <activity android:name=".Signup" >

        </activity>
        <activity android:name=".Adopte" />
        <activity android:name=".Donate" /> <!-- <activity android:name=".Campaign" /> -->
        <activity android:name=".Userdetails" >

        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>